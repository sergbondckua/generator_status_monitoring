# 🎯 Система Моніторингу лампочки (світиться / не світиться)

Автоматична система моніторингу стану лампочки через відеокамеру з сповіщеннями в Telegram.

## 📋 Можливості

- 📹 Моніторинг через відеокамеру
- 💡 Визначення стану лампочки
- 📱 Автоматичні сповіщення в Telegram
- 📊 Логування всіх подій
- 🔄 Автоматичне перепідключення при втраті зв'язку
- 📸 Збереження знімків при зміні стану

## 🏗️ Структура проекта

```
generator_monitor/
├── config/              # Конфігурація
├── interfaces/          # Абстрактні класи
├── hardware/            # Робота з камерою
├── detection/           # Алгоритми визначення
├── notification/        # Система сповіщень
├── visualization/       # Візуалізація
├── core/                # Головний монітор
├── utils/               # Допоміжні утиліти
└── main.py              # Точка входу
```

## 🚀 Встановлення

1. Клонуйте репозиторій або створіть структуру тек
2. Встановіть залежності:

```bash
pip install -r requirements.txt
```

3. Налаштуйте параметри в `main.py`:
   - Логін/пароль камери
   - IP адреса камери
   - ROI координати
   - Telegram Bot Token
   - Telegram Chat ID

## ⚙️ Налаштування

### Камера
```python
camera=CameraConfig(
    username="admin",
    password="your_password",
    ip="192.168.1.100"
)
```

### Визначення
```python
detection=DetectionConfig(
    roi=(430, 410, 150, 100),  # x, y, width, height
    bright_threshold=150,
    min_bright_pixels=3
)
```

### Telegram
```python
telegram=TelegramConfig(
    bot_token="YOUR_BOT_TOKEN_HERE",
    chat_id="YOUR_CHAT_ID_HERE"
)
```

## 🎯 Запуск

```bash
python main.py
```

Для зупинки натисніть `Ctrl+C`

## 🔧 Розширення

### Додати новий тип камери:
```python
from interfaces.base import ICamera

class USBCamera(ICamera):
    # Реалізація методів
    pass
```

### Додати новий детектор:
```python
from interfaces.base import IDetector

class ColorDetector(IDetector):
    # Реалізація методів
    pass
```

### Додати новий нотифікатор:
```python
from interfaces.base import INotifier

class EmailNotifier(INotifier):
    # Реалізація методів
    pass
```

## 📄 Ліцензія

MIT License

## 👨‍💻 Автор
sergbondckua 🇺🇦